/** AsyncStorage: user chose Skip on hot tub onboarding (still nudged in app). */
export const SETUP_SKIPPED_KEY = 'setup_skipped_v1';

/** AsyncStorage: timestamp when user swiped away Finish setup banner (reappears after cooldown). */
export const FINISH_SETUP_DISMISSED_AT_KEY = 'finish_setup_dismissed_at_v1';

/** AsyncStorage: user has seen the one-time Welcome screen after first spa registration. */
export const WELCOME_SEEN_KEY = 'welcome_seen_v1';
