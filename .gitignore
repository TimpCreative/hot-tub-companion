# Dependencies & build output (monorepo; each app may also have its own .gitignore)
node_modules/
**/.next/
**/dist/

# macOS
.DS_Store

# Cursor IDE (local debug / agent artifacts)
.cursor/

# Debug / logs
DebugLogger.txt

# Mobile local secrets (use `eas env:pull` or set vars in shell — see README)
mobile/tenants/*/config.env
mobile/.env
