import { SubscriptionsChrome } from './SubscriptionsChrome';

export default function SubscriptionsLayout({ children }: { children: React.ReactNode }) {
  return <SubscriptionsChrome>{children}</SubscriptionsChrome>;
}
